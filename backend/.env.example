PORT=3000
DATABASE_URL=postgresql://gestorcitas:gestorcitas@localhost:5433/gestorcitas?schema=public
JWT_SECRET=CAMBIAR_ESTE_VALOR
REMINDER_MODE=dev

# SMTP opcional (solo si REMINDER_MODE=email)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@gestorcitas.local
